{
  "name": "shaastra-coord",
  "version": "1.0.0",
  "description": "WebApp to download and submit Shaastra coord apps",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && git npm run build",
    "install-client": "cd client && npm install",
    "heroku-postbuild": "npm run install-client && npm run build",
    "start": "node server.js",
    "client": "cd client && npm start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^7.0.0",
    "express": "^4.16.4"
  },
  "engines": {
    "node": "13.12.0"
  },
  "devDependencies": {
    "nodemon": "^1.18.10"
  }
}
